# Change Manifest — Wire up Jest test infrastructure

BRS: `BRS.md` (repo root)

## Changed Files

| File | Justification |
|---|---|
| `package.json` | Added Jest toolchain devDependencies (AC 1); replaced the stub `test` script with `jest --coverage` (AC 1) |
| `package-lock.json` | Regenerated by `npm install` for the above (no manual edits) |
| `jest.config.js` (new) | Jest configuration: jsdom environment, ts-jest transform via `tsconfig.jest.json`, CSS Modules stub, coverage reporters incl. `json-summary` (AC 1) |
| `jest.setup.js` (new) | Loads `@testing-library/jest-dom` matchers; provides a default `window.matchMedia` stub since jsdom doesn't implement it and `useThemeDetection` calls it on every render (AC 1, needed by AC 2/9) |
| `tsconfig.jest.json` (new) | Extends `tsconfig.json`, adds test-only ambient types (`jest`, `node`, `@testing-library/jest-dom`) without modifying the production `tsconfig.json` (BRS amendment #2) |
| `.gitignore` | Added `coverage/` — generated test output shouldn't be tracked (BRS amendment #3) |
| `global.d.ts` | Net no-op: a triple-slash reference was added then reverted after `tsconfig.jest.json` proved to be the working fix; file is byte-identical to its original state |
| `__tests__/hooks/useThemeDetection.test.ts` (new) | AC 2 |
| `__tests__/hooks/useBusyState.test.ts` (new) | AC 3 |
| `__tests__/hooks/useConfirmation.test.ts` (new) | AC 4 |
| `__tests__/hooks/useZestConfig.test.ts` (new) | AC 5 |
| `__tests__/ZestButtonConfigContext.test.tsx` (new) | AC 6 |
| `__tests__/ZestButtonConfigProvider.test.tsx` (new) | AC 6 |
| `__tests__/SpinnerIcon.test.tsx` (new) | AC 7 |
| `__tests__/semanticTypeButtonConfigDefaults.test.tsx` (new) | AC 8 |
| `__tests__/ZestButton.test.tsx` (new) | AC 9 |
| `BRS.md` (new) | The BRS itself, plus an amendments log recording the two config deviations found during implementation |

No file under `UI/` or `Styles/` (production source) was modified.

## Testing

`npx jest --coverage` — 9 suites, 52 tests, 0 failures. Coverage: 95.38% lines / 94.76% statements / 97.36% functions / 85.23% branches.
`npm run build` — clean, no new diagnostics (only the pre-existing unrelated "mixing named and default exports" rollup warning).
`scripts/verify-post.ps1 -SkipE2E -SkipE2EMarker` — RESULT: COMPLETE. Established `test-reports/coverage-baseline.json` (95.38%, 52 tests).
`scripts/verify-pre.ps1 -SkipE2E` — Phase 0 (Test Reporting Config) and Phase 1 (BRS Verification) now PASS (previously failed/skipped). Remaining failures (Phase 2 protected-branch, Phase 7/8 Next.js-`app/`-folder assumptions) are pre-existing and outside this BRS's scope.

`git config --get remote.origin.url` was checked before and after every step (npm install, file writes, moves, build, both verify scripts) — unchanged throughout: `https://github.com/nyingimaina/jattac.libs.web.zest-button.git`.
